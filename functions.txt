+1. Заполняет таблицу all_users -- add_new_user(user_id_ integer, is_admin_ boolean)
+2. Заполняет таблицу client -- add_new_client(client_id_ integer, client_name_ char, client_email_ char)
+3. Заполняет таблицу items -- add_new_item(item_id_ integer, item_name_ char, item_space_ integer, owner_ integer)
+4. Заполняет таблицу skladi -- add_new_sklad(sklad_id_ integer, owner_id_ integer, free_space_ integer, total_space_ integer, price_for_one_month_ integer)
+5. Заполняет таблицу contracts -- add_new_contract(contract_id_ integer, sklad_id_ integer, client_id_ integer, rent_duration_ integer)
+6. Заполняет таблицу skladi_owner -- add_new_owner(owner_id_ integer, owner_name_ char, owner_email_ char)
+7. Считает общее место, которое занимают вещи клиента -- general_item_space(client_id_ integer)
+8. Выводит доступные клиенту склады, на которых хватит места -- available_sklads(item_space_sum integer)
+9. Выводит информацию о вещах клиента -- info_about_items(client_id_ integer)
+10. Выводит информацию о складах овнеру -- info_about_owner_sklads(user_id_ integer)
+11. Выводит клиенту все его контракты -- info_about_client_contracts(client_id_ integer)
+12. Выводит овнеру все его контракты -- info_about_owner_contracts(owner_id_ integer)
+13. Проверяет админ пльзователь или нет -- is_user_admin(user_id_ integer)
+14. Удаление овнером склада -- del_sklad(sklad_id_ integer)